# 開發紀錄

> Compass §3.2 / §5 追蹤檔。每完成一塊、做出決策、遇到 PRD 問題就 append。
> 範例條目以 `>` 灰底標示，下方空白模板照填即可。

---

## 決策紀錄

> 記錄開發過程中的重大技術/設計決策（日期 / 決策 / 理由）。

> **範例（greyed example，請勿保留）**
> - **2026-06-09** — 採用 SQLite 而非 PostgreSQL
>   - 理由：單機部署、無併發寫入需求，省去 DB server 維運成本；日後可遷移。

- **<日期 YYYY-MM-DD>** — <決策內容>
  - 理由：<為何這樣選；有取捨就寫取捨>

---

## PRD Ambiguity（§5.1.1 採具體一方的紀錄）

> PRD 模糊不清時：採「較具體的一方」繼續寫，並在此標註，事後可回頭核對。

> **範例（greyed example）**
> - **§3.4 登入逾時** — PRD 只寫「逾時要重新登入」，未定義秒數。
>   - 採用：閒置 30 分鐘登出（對齊現有 session 設定）。
>   - 待核：<等使用者確認此數值>

- **<§章節 + 主題>** — <PRD 模糊處描述>
  - 採用：<我選的具體解釋>
  - 待核：<需使用者確認的點>

---

## PRD Issues（§5.1.2 [SKIPPED-PRD] bugs awaiting ruling）

> PRD 本身有 bug / 自相矛盾：停手、記錄、等使用者裁決，**不自行改 PRD**。

> **範例（greyed example）**
> - `[SKIPPED-PRD]` **§4.2 vs §6.1 衝突** — §4.2 說金額可為負，§6.1 又說金額 ≥ 0。
>   - 狀態：等裁決
>   - 影響：付款驗證邏輯暫以 §6.1（≥ 0）止血，待裁決後修正。

- `[SKIPPED-PRD]` **<§章節 + 衝突描述>** — <bug 內容>
  - 狀態：等裁決
  - 影響：<目前暫時怎麼處理 / 卡住了什麼>

---

## PRD Gaps（§5.1.3 缺漏優化 awaiting ruling）

> PRD 沒寫、但實作中發現「補上會更好」：保留實作、記錄、等裁決。
> **不要直接套 YAGNI 砍掉**——交由使用者決定。

> **範例（greyed example）**
> - **匯出功能缺欄位** — PRD 匯出未含「建立時間」，實務報表常需要。
>   - 已實作：暫加 `created_at` 欄位於匯出 CSV。
>   - 狀態：等裁決（保留 / 移除）

- **<主題>** — <PRD 缺漏處與為何補上更好>
  - 已實作：<我做了什麼>
  - 狀態：等裁決（保留 / 移除）

---

## 中斷點

> context 將滿 / 切 session 前必填：當前進度、待辦、下一步從哪接。

> **範例（greyed example）**
> - **2026-06-09 18:30** — 已完成 §3 登入模組（含測試）；§4 付款驗證寫到一半。
>   - 待辦：補 §4 邊界測試、處理上方 PRD Issue 裁決後的修正。
>   - 下一步：從 `payment/validator.ts` 的 negative-amount 分支接續。

- **<日期 時間>** — <已完成到哪>
  - 待辦：<剩什麼>
  - 下一步：<從哪個檔案 / 哪一行接續>
